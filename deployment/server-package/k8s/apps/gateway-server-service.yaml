apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: gateway-server
  name: gateway-server
spec:
  ports:
    - name: "8075"
      port: 8075
      targetPort: 8075
  selector:
    io.kompose.service: gateway-server
