spring:
  application:
    name: user_service
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/task_app

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

keySetURI: ${KEY_SET_URI:http://localhost:8071/oauth2/jwks}
payment:
  yookassa:
    shop-id: 1232637
    secret-key: test_1ICJ7-3M3zk5gGv7s1tPDCK45DfV0nHlbJ6V1eksem8
    return-base-url: ${PAYMENT_RETURN_BASE_URL:http://localhost:5173/payment-return}
server:
  port: 8085
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8070/eureka/
#  instance:
#    prefer-ip-address: true
#management:
#  endpoints.web.exposure.include: prometheus,health,info
#  metrics.tags.application: ${spring.application.name}
